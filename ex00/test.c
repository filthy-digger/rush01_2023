#include "lib.h"
int count_view(int *ch);
int	ft_intcmp(char *s1, char *s2);


int checker(int input[16], int result[16])
{
    size_t size = 16;
    ft_intcmp(input, result);
}



int main(void)
{
    int arr[] = {1, 4, 3, 2,};

    int u_input[16] = {1, 2, 3, 4, 2, 3, 3, 4, 3, 2, 2, 2, 2, 1, 1, 2};

    checker(u_input, )

    printf("%d", count_view(arr));
}